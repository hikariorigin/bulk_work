# このリポジトリでの Claude Code の動作指示

## 前提

柔道整復師養成専門学校の常勤教員が使うリポジトリ。
学校のデジタル化は遅れており、紙・口頭・Excel が主体。
Claude Code はここにある資産と判断基準だけを使って動く。

---

## 基本方針

1. **文面生成は下書き止まり**。最終判断・送信は必ず人間が行う。
2. **個人情報は仮名・記号で扱う**。学生名は「A さん」「3 年 B 組 C」など。
3. **校内提出向けの文体**は「無難・事実ベース・感情を含まない」を優先。
4. **判断基準は `09_判断基準/` を参照**。ここにない判断は勝手にしない。
5. **省エネ優先**。一度聞かれたことは定型文ライブラリに追加提案する。

---

## メニュー一覧と起動方法

### 1. 定型文ライブラリ（`01_定型文ライブラリ/`）
```
状況: [保護者連絡 / 学生連絡 / 提出催促 / 面談案内 / 遅刻欠席対応]
補足: （任意）
```
→ 該当 md のテンプレートを使って文面を出力する。

### 2. 面談記録の整形（`02_面談記録整形/`）
```
メモ: [殴り書きをそのまま貼る]
```
→ 事実 / 気になった点 / 次回対応 / 校内共有向け表現 に分けて出力。

### 3. 会議資料の毒抜き（`03_会議資料毒抜き/`）
```
原文: [会議資料・お達しをそのまま貼る]
```
→ 要求事項 / 制約 / 期限 / 実際にやること / 余計なナラティブ に分解。

### 4. 指導記録の下書き（`04_指導記録/`）
```
誰に: 
いつ: 
何を: 
どう対応した: 
```
→ 校内提出用の記録文を出力。

### 5. 小テスト・配布資料（`05_小テスト資料/`）
```
科目: 
単元: 
レベル: [基礎 / 標準 / 応用]
種別: [小テスト / ワークシート / 説明プリント / 確認問題]
```
→ 初稿を出力。

### 6. 評価コメント支援（`06_評価コメント/`）
```
学生メモ: [成績・観察メモを貼る]
用途: [通知表 / 面談 / 内部記録]
```
→ コメント候補・努力点・課題点を出力。最終判断は人間。

### 7. 提出物・名簿データ整理（`07_提出物管理/`）
```
データ: [CSV や表を貼る]
処理: [未提出一覧 / 再提出対象 / 出席並び替え / 印刷用整形]
```
→ 処理結果を出力。

### 8. 年度またぎ資産化（`08_年度資産化/`）
```
種別: [文面 / 資料 / 指導メモ]
科目 / 行事 / 学年: 
内容: [保存したい内容を貼る]
```
→ 適切なフォルダに保存用 md を提案。

### 9. 判断基準の参照・追加（`09_判断基準/`）
```
操作: [参照 / 追加 / 修正]
項目: 
内容: （追加・修正時）
```
→ 基準集を読み書きする。

---

## 出力フォーマット共通ルール

- 見出しは `##` まで。深い階層は使わない。
- 箇条書きは `-` 統一。
- 「〜と思われます」「〜かもしれません」は使わない。事実と判断を分離して書く。
- 敬語は「です・ます」調。校内文書は「である」調も可（指示に従う）。
- 生成物の末尾に必ず「※ 要確認箇所:」セクションを付ける。

---

## やらないこと

- 保護者・学生への直接送信
- 懲戒・退学に関する文面の単独生成
- 法的助言
- 個人特定につながる情報の外部サービスへの送信
